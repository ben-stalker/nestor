version: "6.0.0"
project_name: "Nestor"
project_type: web-app
project_level: 3
last_updated: "2026-05-17-epic14"

phases:
  phase_1_analysis:
    status: complete
    workflows:
      product_brief: skipped
      research: skipped
      brainstorm: skipped

  phase_2_planning:
    status: complete
    workflows:
      prd:
        status: complete
        file: nestor-prd.md
      tech_spec: skipped
      ux_design: pending

  phase_3_solutioning:
    status: complete
    workflows:
      architecture:
        status: complete
        file: docs/architecture-nestor-2026-05-08.md
      gate_check: pending

  phase_4_implementation:
    status: in_progress
    workflows:
      sprint_planning:
        status: complete
        file: docs/sprint-plan-nestor-2026-05-08.md
      dev_stories:
        status: in_progress
        completed:
          - STORY-1.1
          - STORY-1.2
          - STORY-1.3
          - STORY-1.4
          - STORY-1.5
          - STORY-1.6
          - STORY-1.7
          - STORY-1.8
          - STORY-1.9
          - STORY-1.10
          - STORY-1.11
          - STORY-2.1
          - STORY-2.2
          - STORY-2.3
          - STORY-2.4
          - STORY-2.5
          - STORY-2.6
          - STORY-2.7
          - STORY-2.8
          - STORY-2.9
          - STORY-2.10
          - STORY-2.11
          - STORY-2.12
          - STORY-3.1
          - STORY-3.2
          - STORY-3.3
          - STORY-3.4
          - STORY-3.5
          - STORY-3.6
          - STORY-3.7
          - STORY-3.8
          - STORY-3.9
          - STORY-4.1
          - STORY-4.2
          - STORY-4.3
          - STORY-4.4
          - STORY-4.5
          - STORY-4.6
          - STORY-4.7
          - STORY-4.8
          - STORY-4.9
          - STORY-4.10
          - STORY-4.11
          - STORY-4.12
          - STORY-3.5
          - STORY-3.7
          - STORY-5.1
          - STORY-5.2
          - STORY-5.3
          - STORY-5.4
          - STORY-5.5
          - STORY-5.6
          - STORY-5.7
          - STORY-5.8
          - STORY-5.9
          - STORY-5.10
          - STORY-6.1
          - STORY-6.2
          - STORY-6.3
          - STORY-6.4
          - STORY-6.5
          - STORY-6.6
          - STORY-6.7
          - STORY-6.8
          - STORY-7.1
          - STORY-7.2
          - STORY-7.3
          - STORY-7.4
          - STORY-7.5
          - STORY-7.6
          - STORY-8.1
          - STORY-8.2
          - STORY-8.3
          - STORY-8.4
          - STORY-8.5
          - STORY-8.6
          - STORY-8.7
          - STORY-8.8
          - STORY-8.9
          - STORY-8.10
          - STORY-9.1
          - STORY-9.2
          - STORY-9.3
          - STORY-9.4
          - STORY-9.5
          - STORY-9.6
          - STORY-9.7
          - STORY-7.7
          - STORY-7.8
          - STORY-7.9
          - STORY-7.10
          - STORY-7.11
          - STORY-7.12
          - STORY-10.1
          - STORY-10.2
          - STORY-10.3
          - STORY-10.4
          - STORY-10.5
          - STORY-10.6
          - STORY-11.1
          - STORY-11.2
          - STORY-11.3
          - STORY-11.4
          - STORY-11.5
          - STORY-11.6
          - STORY-12.1
          - STORY-12.2
          - STORY-12.3
          - STORY-12.4
          - STORY-13.1
          - STORY-13.2
          - STORY-13.3
          - STORY-13.4
          - STORY-13.5
          - STORY-14.1
          - STORY-14.2
          - STORY-14.3

backlog: []

current_phase: phase_4_implementation
recommended_next: dev_stories
notes: >
  PRD provided by user (nestor-prd.md). Architecture document generated 2026-05-08.
  STORY-12.1–12.4 complete (2026-05-17): Epic 12 — Contacts Module. Migration 017_contacts.sql
  (contacts table with category CHECK enum: medical/school/pets/home_services/emergency/family/trade/other,
  linked_pet_id + linked_vehicle_id FKs). Migration 018_pets_vet_contact.sql (vet_contact_id FK on pets).
  ContactRepository (CRUD + list({ category })). createContactsRouter (GET list with child=emergency-only
  filter, GET/:id, POST/PATCH/DELETE admin-pin gated). Client: types, api, useContacts hook, ContactCard
  (initials avatar, tel: link, long-press 600ms clipboard copy), ContactForm, ContactsList (collapsible
  sections, search by name/role, admin CTA), ContactsPage (/contacts route), ContactPicker (category
  select + inline add-new modal). navModes updated: contacts nav entry + view_contacts permission gate.
  PetFormModal + MaintenanceForm updated with ContactPicker. 21 new server tests (954 total),
  20 new client tests (521 total). Lint + typecheck clean.
  STORY-11.1–11.6 complete (2026-05-16): Epic 11 — Board Module. Migration 016_board.sql
  (board_messages + countdown_timers + whiteboard_snapshots tables). BoardMessageRepository,
  CountdownRepository, WhiteboardRepository. createBoardRouter (messages CRUD + archive,
  countdowns CRUD, whiteboard PNG upload/serve/delete, board lists reusing checklist engine,
  guest checklists with arrival/departure templates). evaluateGuestAlerts service (7/3/1-day
  alerts before guest arrival if incomplete). board:message_new event bus + WS forward.
  Client: BoardPage (5 tabs), MessageBoard (sticky notes + author colour + real-time WS push),
  Whiteboard (HTML5 canvas + pointer events + undo + 9 colours + snapshot gallery),
  CountdownList (day chips + form modal), ListsTab (one_off/recurring + item CRUD + reset),
  GuestVisitTab (arrival/departure templates + item ticking). Router updated /board → BoardPage.
  28 new server tests (933 total), 21 new client tests (501 total). Lint + typecheck clean.
  STORY-10.1–10.6 complete (2026-05-16): Epic 10 — Pets Module. Migration 015_pets.sql
  (pets + pet_health_logs tables, partial index idx_pet_health_next_due). PetRepository
  (CRUD + soft delete) + PetHealthLogRepository (CRUD + upcomingCare query). Zod types
  in server/src/types/pets.ts. petAlertService: pet_care alerts with deduplication via
  alertKey. 11 REST endpoints (photo upload via multer+sharp→webp 800px, document upload
  PDF/JPEG/PNG). Calendar event integration for vet_visit log type (linked_calendar_event_id).
  Scheduler reminder-eval extended with evaluatePetAlerts. Client module: types, api,
  3 hooks, PetsPage (two-column portrait/landscape), PetList, PetCard, PetDetail (3 tabs),
  PetFormModal, HealthLogList, HealthLogEntry, HealthLogFormModal, WeightChart (SVG polyline),
  DocumentList. Pets registered as filtersStore plugin. navModes.ts already had pets entry.
  47 new tests (25 server / 22 client). Server 898 total, client 476 total. Lint + typecheck clean.
  STORY-7.7–7.12 complete (2026-05-16): Epic 7 finished — migration 014
  (profiles.dob/feed_alert_hours/conversion_rate), baby tracking (BabyView quick-log
  buttons + baby-summary endpoint + feed-overdue alert service), growth chart (WHO
  percentile SVG + growth-data endpoint), NHS vaccination schedule (17 vaccines,
  VaccinationService + vaccinations endpoint + scheduler alerts), children's routines
  (RoutinesPanel reuses checklist system), mood log (MoodCheckin 5-emoji + MoodTrend SVG
  bar chart + mood-trend endpoint), allowance tracker (conversion_rate per profile,
  moneyEquivalent in rewards grid). Jest moduleNameMapper added for .js → .ts resolution.
  16 new server tests (878 total), 11 new client tests (465 total). Lint + typecheck clean.
  STORY-9.5–9.7 complete (2026-05-16): Finance module extended — end-date alerts
  (finance reminders service, scheduler extension, admin run-reminder-eval endpoint),
  regular commitments (migration 013, CRUD API + client UI with in/out direction,
  day_of_month, category, net surplus/deficit display, included in summary),
  debt paydown visualiser (amortisation schedule endpoint, SVG bar chart, toggle
  on AgreementList cards). 19 new server tests (846 total). TypeScript + lint clean.
  STORY-9.1–9.4 complete (2026-05-16): Finance module — migration 012_finance.sql
  (finance_agreements/savings_goals), FinanceRepository (CRUD + monthly aggregation),
  finance routes (agreements CRUD + savings CRUD + GET /summary aggregating agreements
  + subscriptions), FinancePage with 3 tabs (Agreements/Savings Goals/Summary),
  AgreementList/AgreementForm (PCP balloon_payment, mortgage fixed_rate_end_date),
  SavingsGoalList/SavingsGoalForm (progress bar, target date countdown),
  CommitmentsSummary (expandable per-category subtotals). 34 new server tests (827 total),
  16 new client tests (443 total). Lint + typecheck clean.
  STORY-8.1–8.10 complete (2026-05-16): House module — migration 011_house.sql
  (bin_schedules/subscriptions/home_maintenance/meter_readings/budget_categories/
  budget_expenses/checklists/checklist_items), seven repositories (BinSchedule,
  Subscription, HomeMaintenance, MeterReading, Budget, Checklist; ChoreRepository
  updated with adultOnly filter), binSchedule service (nextCollections with UK BH
  shift 2024-2027), binAlertService, bins/subscriptions/maintenance/meterReadings/
  budget/checklists routes, HousePage with 7 tabs (Bins/Subscriptions/Maintenance/
  Meter/Budget/Checklists/Chores), 7 seeded checklist templates, checklist-reset
  scheduler job. 92 new server tests (793 total), 23 new client tests (427 total).
  Lint + typecheck clean.
  STORY-7.1–7.6 complete (2026-05-15): Family module — migration 010_family.sql
  (chores/completions/redemptions/health_logs), four repositories, ChoreService,
  chores + rewards + family + health-log routes, pdf-lib export, FamilyHub +
  ChildCard + ChildDetail + HealthLog + HealthLogAddModal client components,
  MePage + ChoreTile + RewardStarGrid + ToddlerView child views, router updated
  with HomeRedirect/MeRoute. 36 new server tests (701 total), 30 new client tests
  (404 total). Lint + typecheck clean.
  Sprint plan generated 2026-05-08 — 178 stories across 20 epics, 14 sprints,
  112 stories in MVP (Sprints 1–10, ~20 weeks single-developer).
  STORY-1.1 complete (2026-05-09): monorepo scaffold, npm workspaces, TypeScript 6,
  ESLint 8/Airbnb, Prettier 3, Husky 9, lint-staged 17. All 4 scripts passing.
  Architecture doc updated to latest library versions (React 19, Express 5, Node 22 LTS, etc).
  STORY-1.2 complete (2026-05-09): GitHub Actions CI, Dependabot, issue/PR templates.
  STORY-1.3 complete (2026-05-09): better-sqlite3 singleton connection, WAL mode pragmas,
  numbered SQL migration runner, applied_migrations tracking table, db:reset script,
  8 unit tests passing (connection + migrationRunner suites). ESLint updated with
  tsconfigRootDir + server/tsconfig.test.json for test file coverage.
  STORY-1.4 complete (2026-05-09): Express server bootstrap with pino structured logging.
  createApp() factory, requestId middleware, errorHandler middleware, pino-http logger,
  GET /health with DB ping, graceful SIGTERM/SIGINT shutdown (10s timeout), uncaught
  exception handlers. 12 tests passing (3 suites). Fixed pre-existing prettier issues.
  STORY-1.5 complete (2026-05-09): BaseRepository abstract class (protected get/all/run
  helpers), AppSettingsRepository with in-memory cache (null-out invalidation on write),
  migration 001_app_settings.sql, settings-keys.ts with Zod schemas for 19 known keys
  plus validateSetting helper. 19 new tests (31 total passing).
  STORY-1.6 complete (2026-05-09): Vite 8 + React 19 + Tailwind v4 frontend bootstrap.
  @tailwindcss/vite plugin, react-router-dom v7, BrowserRouter + Routes placeholder,
  Nestor splash screen, Express static serving for production, tsx watch dev script,
  concurrently root dev script, 2 RTL tests (33 total passing).
  STORY-1.7 complete (2026-05-09): TanStack Query + Zustand + global ErrorBoundary.
  QueryClientProvider (30s stale, retry 1, no refetchOnWindowFocus), ReactQueryDevtools
  (dev-only), useAppStore Zustand slice (activeProfileId, adminPin, alertCount,
  voiceStatus), apiFetch wrapper (X-Profile-Id/X-Admin-Pin headers), ErrorBoundary
  with recovery card + POST /api/v1/client-errors logging. 46 total tests passing.
  STORY-1.8 complete (2026-05-09): AES-256-GCM encryption helper for credentials at rest.
  CryptoService class + module-level encrypt/decrypt exports. HKDF-SHA256 key derivation
  from machine-id + auto-generated 32-byte salt stored in app_settings.encryption_salt.
  getMachineId() with /etc/machine-id → ~/.nestor/machine-id (UUID, mode 0600) fallback.
  Output format v1:<iv_b64>:<tag_b64>:<ct_b64>; GCM auth tag tamper detection.
  17 new tests (48 server total, 63 overall). lint + typecheck clean.
  STORY-1.9 complete (2026-05-10): Typed in-process event bus singleton. TypedEventBus
  wraps Node EventEmitter with generic emit/on/off/once over EventMap. Custom dispatch
  loop (forEach + try/catch) isolates listeners — a throwing handler logs via pino and
  remaining listeners still run. setMaxListeners(50). EventMap declared in eventBus.types.ts
  with placeholder Alert type; 8 events covered. AppSettingsRepository updated to emit
  settings:updated on set/delete. 11 new tests (59 server total). lint + typecheck clean.
  STORY-1.10 complete (2026-05-10): WebSocket server scaffold. NestorWsServer class mounts
  ws v8 on the Express HTTP server at /ws. broadcast(msg) + send(clientId, msg) exposed.
  Subscribes to alert:new, alert:dismissed, voice:status, calendar:synced from event bus.
  30s heartbeat ping with dead-client pruning. Configurable heartbeat interval for tests.
  Client-side useWebSocket() hook with exponential back-off (500ms base, 30s cap, 2x factor).
  11 new server tests (70 total), 8 new client tests (24 total). lint + typecheck clean.
  ESLint updated: no-void allowAsStatement: true (resolves no-floating-promises conflict).
  STORY-1.11 complete (2026-05-10): Job scheduler base. Scheduler class wraps node-cron v4.
  register(name, cron, handler) — validates expression, wraps in try/catch, tracks runCount/
  lastRun/lastError. runNow(name) for immediate execution. list() returns JobInfo[].
  stop() halts all jobs on shutdown. Five built-in placeholder jobs: weather-refresh (*/30),
  caldav-sync (*/15), reminder-eval (5 0 *), github-update-poll (0 3 *), vacuum-db (0 3 * * 0).
  node-cron installed (v4.2.1). 23 new tests (93 server total). lint + typecheck clean.
  STORY-2.1 complete (2026-05-10): Profiles schema and repository. Migration 002_profiles.sql
  creates profiles table with full schema per architecture data model. ProfileRepository
  extends BaseRepository with list/get/create/update/delete/verifyPin. bcrypt cost 10 for
  PIN hashing. LastAdminError (code LAST_ADMIN) thrown on last-admin delete. Zod validation
  on colour hex, name, type enum, text_size enum. JSON columns (accessibility_json,
  permissions_json) parsed on read. BaseRepository.get renamed to queryOne to allow
  ProfileRepository.get(id). bcrypt + @types/bcrypt installed. 28 new tests (121 server total).
  lint + typecheck clean.
  STORY-2.2 complete (2026-05-10): Profile API endpoints. createProfilesRouter() factory
  mounts 6 REST endpoints under /api/v1/profiles: GET list, POST create, PATCH update,
  DELETE (LastAdminError → 400 LAST_ADMIN), POST /:id/verify-pin (rate-limited 5/15min
  via express-rate-limit), GET /:id/permissions. Zod validation on all inputs.
  requireAdminPin stub (next() passthrough; TODO for STORY-2.3). pin_hash never exposed
  in any response. express-rate-limit v8 installed. 22 new tests (143 server total).
  lint + typecheck clean.
  STORY-2.3 complete (2026-05-10): Profile and admin-PIN middleware.
  requireProfile middleware (X-Profile-Id header → req.profile, 401 UNKNOWN_PROFILE on
  missing/invalid). requirePermission(permKey) (403 PERMISSION_DENIED with details).
  createRequireAdminPin with 15s in-memory cache of admin pin_hashes via new
  ProfileRepository.listAdminPinHashes(). Express.Request augmented with profile?: Profile
  via server/src/types/express.d.ts. profiles.ts stub replaced with real middleware;
  route factory accepts injected adminPinMiddleware for testability. 20 new tests
  (163 server total). lint + typecheck clean.
  STORY-2.4 complete (2026-05-10): Permission matrix defaults per profile type.
  server/src/services/permissionDefaults.ts — pure defaultsFor(type) function encoding
  PRD §5 matrix for all 7 ProfileTypes × 29 PermissionKeys. row() helper builds from
  granted-set; all others false. POST /api/v1/profiles applies defaults when
  permissions_json is absent; explicit body overrides. Snapshot tests (7 snapshots, one
  per type) + completeness + immutability + integration tests. 22 new tests (185 server
  total). lint + typecheck clean.
  STORY-2.5 complete (2026-05-10): Design system tokens and core primitives.
  Tailwind v4 @theme block in index.css — 10 mode accents, 12 profile colours, 4 alert
  colours, text scale (display/h1/h2/body/caption), radii (card 24px, button 16px).
  Inter self-hosted via @fontsource/inter (400/500/600/700/800). Dark-mode overrides on
  [data-theme=dark]. 9 primitive components in client/src/shared/ui/: Card, Button,
  TouchTarget, Modal (react-focus-lock + Escape close), Pill, Avatar (initials fallback),
  IconButton, Skeleton (animate-pulse + motion-reduce:animate-none), EmptyState.
  useReducedMotion hook. Lazy /ui gallery route (DEV-only). 33 new client tests
  (57 client total; 185 server total). lint + typecheck clean.
  STORY-2.6 complete (2026-05-10): App shell portrait/landscape CSS grid layout
  with navbar/rail/filters/main grid areas. prettier formatting fixes.
  STORY-2.7 complete (2026-05-10): Bottom navbar / side rail with mode buttons.
  lucide-react installed. navModes.ts defines 10 default modes with icons + accent
  colours. NavBar component: portrait single/double/scrollable/hamburger layouts;
  landscape vertical rail. Reads enabled_nav_modes + nav_layout from app_settings.
  NavLink active state + aria-current="page". Per-mode badge counts via
  appStore.badgeCounts. Hamburger More sheet uses Modal. 30 new client tests
  (87 client total; 185 server total). lint + typecheck clean.
  STORY-2.8 complete (2026-05-10): Profile context + profile switcher avatar strip.
  Server: pinSet: boolean added to Profile type (SQL CASE WHEN pin_hash IS NOT NULL).
  Client: ApiError class with status code in apiFetch. Zustand persist middleware
  for activeProfileId (localStorage). useProfiles() TanStack Query hook,
  useActiveProfile() derived hook, applyProfileSettings() sets data-text-size +
  data-simplified-nav on html element. ProfileProvider initializes + syncs active
  profile. AvatarStrip: horizontal scrollable strip, colour border, tap-to-switch,
  PIN gate. PinPrompt: numeric keypad, 4-dot indicator, CSS shake keyframe,
  429 "Too many attempts" surfacing, prefers-reduced-motion aware.
  CSS: text-size overrides, simplified-nav, avatar-strip layout, shake keyframe.
  AvatarStrip placed on home page header. 24 new client tests
  (111 client total; 185 server total). lint + typecheck clean.
  STORY-2.9 complete (2026-05-10): Filter panel sidebar/strip. filtersStore
  (Zustand persist, per-profile keyed filtersByProfile). FilterPanel component:
  portrait=88px left sidebar, landscape=top strip. "All" toggle, profile pills
  colour-coded, plugin filter registry (registerPlugin/unregisterPlugin). App
  shell CSS grid updated: portrait now 88px filters column + main + navbar;
  landscape now rail + filters/main stacked rows. 33 new client tests
  (144 client total; 185 server total). lint + typecheck clean.
  STORY-2.10 complete (2026-05-10): Kiosk-child mode. KioskLockSchema changed
  to string|null (profile ID). kioskLock middleware (403 KIOSK_LOCKED) applied
  to profiles write routes. Admin router: POST /kiosk-lock (activate) + POST
  /kiosk-unlock (verify any admin PIN + clear) — both exempt from kiosk middleware.
  KioskOverlay: lock icon indicator + hidden bottom-right triple-tap (3×/800ms)
  → AdminPinPrompt → invalidates settings on success. AvatarStrip disabled with
  avatar-strip--locked class. NavBar filters modes via NAV_MODE_PERMISSION map
  against locked profile permissions. 14 new server tests (199 total), 17 new
  client tests (161 total). lint + typecheck clean.
  STORY-2.11 complete (2026-05-10): Guest lock-screen overlay. Server: POST
  /api/v1/admin/verify-pin (side-effect-free admin PIN check, rate-limited).
  Client: guestProfileId + setGuestMode in appStore (not persisted). verifyAdminPin
  API call. AdminPinPrompt extracted to shared/ui/AdminPinPrompt.tsx with generic
  onVerify prop — KioskOverlay updated to use it. GuestOverlay: fixed full-screen
  overlay with header (avatar + name + today's date), 2×2 grid of placeholder
  sections (Today's Events, Child Routines, Emergency Contacts, Today's Meal),
  exit button → AdminPinPrompt → clears guestProfileId. AvatarStrip: guest-type
  profile click activates guest mode (with optional PIN gate) instead of switching.
  AppShell renders GuestOverlay. CSS guest-overlay layout. 5 new server tests
  (204 total), 19 new client tests (180 total). lint + typecheck clean.
  STORY-3.1 complete (2026-05-12): Open-Meteo weather service + cache. WeatherService
  fetches Open-Meteo (no key) caching {current,hourly,daily} for 7 days. Scheduler
  weather-refresh job uses location from app_settings. GET /api/v1/weather serves cache.
  Stale cache on failure; alert:new after 6h. 18 new server tests (225 total).
  STORY-3.2 complete (2026-05-12): Home route layout. HomePage replaces placeholder at /
  with HomeHeader (greeting, live clock, locale date, weather widget), AvatarStrip, and
  placeholder sections. useWeather hook (25min stale). 9 new client tests (208 total).
  STORY-3.3 complete (2026-05-12): Day carousel. DayCarousel renders yesterday+today
  (focal ~50%)+4 days (thin, scroll-snap). DayCard: date/weather/events. Framer Motion
  LayoutGroup+AnimatePresence. DayViewModal+QuickAddModal. Back to Today pill.
  prefers-reduced-motion support. 9 new client tests (217 total).
  STORY-3.9 complete (2026-05-12): "Coming Up" widget. GET /api/v1/home/coming-up
  stub endpoint returning ComingUpItem[] (sorted by daysUntil, max 3). ComingUpWidget
  renders up to 3 items with day-count chips; hides when empty; navigates on tap.
  3 new server tests (264 total), 6 new client tests (263 total). lint+typecheck clean.
  STORY-3.4 complete (2026-05-12): Day-summary endpoint + badge strip.
  GET /api/v1/home/day-summary?date= stub aggregation endpoint. DayCard badge strip:
  WFH/office/nursery/school/vehicle/vet/bin icons, filter-gated. useDaySummary hook.
  4 new server tests (229 total), 6 new client tests (223 total). lint+typecheck clean.
  STORY-4.1 complete (2026-05-12): Calendar accounts and events schema.
  Migration 005_calendar.sql: calendar_accounts + calendar_events with all columns,
  FKs, and 3 indexes (idx_events_profile_date, idx_events_account, idx_events_caldav_uid).
  CalendarAccountRepository (CRUD + getCredentials encrypt/decrypt + markSynced + listActive).
  EventRepository (CRUD + findInRange + upsertByCaldavUid + findRecurring). AccountInput
  uses z.input<> for optional-with-default fields. 27 new tests (291 total).
  STORY-4.2 complete (2026-05-12): Local event CRUD endpoints.
  POST/PATCH/DELETE/GET /api/v1/calendar/events. requireProfile + requirePermission
  (add/edit/delete_calendar_event). CalDAV-source events read-only (403) unless admin
  + x-admin-pin header. Zod validation on all inputs. Router mounted in app.ts.
  16 new route tests (307 total). lint + typecheck clean.
  STORY-4.3 complete (2026-05-12): Recurring event expansion via ical.js.
  expandRecurring() default export uses ical.js RFC-5545 iterator. Supports DAILY/
  WEEKLY/MONTHLY/YEARLY with BYDAY, INTERVAL, COUNT, UNTIL. Invalid RRULEs return [].
  EventRepository.findInRange partitions single + recurring, expands in-memory, merges
  and sorts. occurrence_id = id::epochMs. ical.js installed. 11 new tests (318 total).
  STORY-4.4 complete (2026-05-12): CalendarService skeleton + sync interface.
  CalendarProvider interface (pull/push/testCredentials). LocalProvider no-op object.
  providerRegistry. CalendarService.syncAccount: pull + upsertByCaldavUid + markSynced
  + emit calendar:synced. syncAllAccounts uses Promise.allSettled. caldav-sync scheduler
  job wired to syncAllAccounts (replaces placeholder). 9 new tests (327 total).
  STORY-4.5 complete (2026-05-13): Google Calendar OAuth2 + QR pairing. Device code
  flow (google_oauth_client_id/secret from settings or env). QR PNG via qrcode.
  deviceCodeStore (in-memory TTL Map). Routes: POST /google/start + GET /google/poll/:code.
  GoogleCalDAVProvider (Bearer auth via tsdav, token refresh, ICAL parsing). ical.d.ts
  ambient module declaration fixes ESM/CJS interop for ical.js in TypeScript Node16.
  21 new server tests (348 total).
  STORY-4.6 complete (2026-05-13): Apple iCloud + Yahoo CalDAV adapters. BasicAuthCalDAVProvider
  with fetchBasicAuthCalDAV/testBasicAuthCalDAV helpers. Routes: POST /basic/test + POST /basic.
  Default URLs: caldav.icloud.com (Apple), caldav.calendar.yahoo.com (Yahoo). Credentials
  encrypted at rest. 12 new server tests (348 total). lint + typecheck clean.
  STORY-4.7 complete (2026-05-13): Calendar Day view. layoutOverlaps greedy interval-graph
  colouring. HourGutter (06–22, 64px/hr). EventBlock absolutely-positioned. DayView with
  TanStack Query, all-day strip, profile colour, slot quick-add. CalendarPage date navigation.
  /calendar route wired. 18 new client tests (281 total). lint + typecheck clean.
  STORY-4.8 complete (2026-05-13): Calendar Week view. WeekHeader Mon-anchored 7-column,
  today aria-current. WeekGrid 7 WeekColumn components reusing layoutOverlaps/EventBlock.
  WeekView week-range query, eventsByDay Map. CalendarPage Day/Week tab strip (tablist).
  9 new client tests (290 total). lint + typecheck clean.
  STORY-4.9 complete (2026-05-13): Calendar Month view. buildMonthGrid 6×7 42-cell grid
  (Mon-first). MonthCell with date number, up to 3 colour dots (custody stripe variant),
  +N overflow. Today ring highlight. Tap cell → onDayClick → CalendarPage switches to day.
  Prev/Next month navigation. CalendarPage gains Month tab. 14 new client tests (304 total).
  STORY-4.10 complete (2026-05-13): Event detail modal + quick-add modal. EventModal with
  create/edit/view modes replaces inline EventDetailModal/QuickAddSheet in DayView and
  WeekView. RecurrencePicker (daily/weekly/monthly/yearly presets + weekday chip row).
  calendar/api.ts with createEvent/updateEvent/deleteEvent mutations. Delete confirmation
  inline. CalDAV read-only banner for non-admin. Form validation with field-level errors.
  11 new client tests (311 total). Prettier + lint + typecheck clean.
  STORY-4.11 complete (2026-05-13): Term dates iCal subscription. Migration 006 adds
  term_dates_ical_url TEXT to profiles. ProfileRepository + UpdateProfileSchema updated.
  TermDatesService: fetches iCal per child profile, parses with ical.js ICAL.Event API,
  upserts school_term events via findByCaldavUid+create/update. Inset day detection creates
  alerts (deduped). Scheduler nightly job (0 2 * * *) registered. 6 new server tests.
  STORY-4.12 complete (2026-05-13): Custody schedule entry type. Server route filters
  custody events from non-admin profile responses. CSS diagonal stripe for event-block--custody
  and month-cell__dot--custody. EventBlock applies custody class. custody_label added to
  SETTING_SCHEMAS. 3 new server tests (357 total). Prettier clean.
  STORY-5.1 complete (2026-05-14): Food schema + repos. Migration 007_food.sql creates
  recipes, recipe_ingredients, meal_plan, shopping_items tables with FK constraints and indexes
  (idx_meal_plan_date, idx_ingredients_recipe, idx_shopping_ticked). server/src/types/food.ts
  with Zod schemas. RecipeRepository (CRUD + search by title/tags + ingredient management),
  MealPlanRepository (listForRange with LEFT JOIN recipe, upsert by date+slot),
  ShoppingItemRepository (CRUD + clearTicked). 33 new server tests (390 total).
  STORY-5.2 complete (2026-05-14): Recipe API + photo upload. multer + sharp installed.
  createRecipesRouter factory: GET/POST/PATCH/DELETE /api/v1/recipes, GET /api/v1/recipes/:id,
  POST /api/v1/recipes/:id/photo (10MB limit, sharp resize 1200px → webp, stored at
  ~/.nestor/uploads/recipes/<uuid>.webp), GET /api/v1/recipes/:id/photo (file streaming),
  POST /api/v1/recipes/import-url stub. Router mounted in app.ts. 18 new tests (408 total).
  STORY-5.3 complete (2026-05-14): Recipe URL import via Schema.org JSON-LD. cheerio installed.
  recipeScraper.ts: fetches URL with 10s timeout, parses JSON-LD @type:Recipe (including
  @graph), extracts title/description/ingredients/method/prep/cook/servings/calories/photo.
  ISO 8601 duration parser (PT1H30M → 90). Ingredient regex tokeniser with fraction support.
  Partial fallback when no JSON-LD. url_import_warned setting for one-time warning.
  import-url route wired to real scraper. 9 new scraper tests (455 total server; 37 suites).
  STORY-5.4 complete (2026-05-14): Meal planner 7-day grid. Server: createMealPlanRouter with
  GET/POST/DELETE /api/v1/meal-plan; upsert by plan_date+slot_name; inline recipe via LEFT JOIN.
  Client: client/src/food/ module — types.ts, api.ts, useMealPlan.ts (TanStack Query),
  MealPlanner.tsx (7-column Mon-Sun grid, today highlight, week navigation, slot editor with
  recipe picker or free text), RecipePickerModal.tsx, FoodPage with Planner/Recipes tabs.
  /food route in router.tsx replaced. 15 new server tests + 8 client tests (455 server / 354 client).
  STORY-5.5 complete (2026-05-14): Recipe library list + detail views. RecipeList.tsx (debounced
  search, tag filter pills, card grid), RecipeCard.tsx (photo/placeholder, time chips, tags),
  RecipeDetail.tsx (hero photo, checkable ingredients scaled by servings, numbered steps, admin
  gate for edit/delete), ServingsScaler.tsx (stepper 1-99, scales ingredient quantities),
  RecipeFormModal.tsx (create/edit with dynamic ingredients, import-URL pre-fill, photo upload),
  useRecipes.ts hook. Two-column landscape layout in FoodPage. 29 new client tests (354 total;
  40 suites). lint + typecheck clean.
