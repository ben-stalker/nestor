{
  "name": "@nestor/client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet\" && exit 0",
    "build": "echo \"Vite bootstrap in STORY-1.4\" && exit 0"
  },
  "devDependencies": {}
}
